# AUDIT — 2026-07-18 (the standing accuracy prompt, run #3)

Five parallel hunters over the whole codebase, hostile-review framing ("checking another AI's
work" — i.e. the v680–v720 changes got the harshest light). Every finding verified against
source before repair; every repair verified by typecheck + build + the engine test suites
(334/334, UTC **and** America/Los_Angeles).

## CRITICAL — 1 found, 1 fixed
- **verdict.ts NaN cascade**: `fruitOf` divided the `vimshopak.points` RECORD by 20 → NaN →
  every chart on earth read "late-if-at-all", hinge never strength-picked. Fixed to
  `.points.shodasha` (as every other consumer reads it). Self-heals via hash; no locked rows
  existed.

## HIGH — 4 found, 4 fixed
- **recentReads hashed** (cache-law violation): opening free YESTERDAY changed TODAY's hash →
  silent re-bill + different prose at noon. recentReads now stripped from the hash (model still
  receives it). NOTE: one-time cache bust of glance/day/cast rows on deploy (formula change).
- **verdict bloomYear off-by-one** for late-year births → year now read from the window's own
  startAt.
- **timeLordTransit.calculate unbounded** (~3.65M ephemeris calls possible in one request) →
  date-format lock + 400-day span cap + inverted-range rejection.
- **profiles.calculateChart skipped the 24h birth-data cooldown** (the anti-hijack side door;
  each swap also burned research + LLM warms) → cooldown enforced + lock stamped on change.

### Client HIGHs — 3 found, 3 fixed
- **Golden-day border computed but never drawn** since the coin refactor → restored (gold ring).
- **Gentle-overdue label was dead code** — no surface showed a due date at all → wired as a
  whisper row (CalendarDays + "waiting since Jul 8") in the same register as the snooze row.
- **Bindi tracks dropped the SLOW planets first** (canonical slice) — a Saturn station could
  show nothing while a Mercury pre-shadow rendered → selection now by peak month strength,
  ties slow-first; row order stays canonical.

## MEDIUM — 12 found, 11 fixed, 1 deferred
- Free window granted TOMORROW (and +2 in western evenings) → tz-honest window: exactly local
  yesterday+today when the viewer's timezone is stored; wide UTC fallback otherwise.
- tlWindowRead `?? rows[0]` could voice ANOTHER profile's engine data and cache it → removed.
- horoscope.reveal: date format-locked (the VARCHAR outage class) + failed freezes recorded to
  the black box (were silently swallowed while re-reveals re-billed).
- Pin split-brain: isNarrativeLocked now reads BOTH surfaces (a deep-only lock was invisible).
- panchang.recalculate → protected + rate-limited; diagnostics.day → protected; byMonth/byDate
  rate-limited + format-locked.
- Profection calculator UTC half-fix completed (yearEnd/formatDate) — the 2 red tests now pass
  under any host TZ.
- verdict dignity friend/enemy unreachable → neutral states refined through signDignity.
- verdict nodes structurally excluded (Rahu/Ketu mahas could never bloom) → node occupying an
  area house joins at documented 0.5 fruit.
- verdict past windows voiced in future tense → per-area `tense` + prompt instruction.
- DueOrbSheet optimistic update wrote to the wrong cache key (no-op) → keyed correctly.
- BrandSplash timers reset on every parent re-render → ref'd callback; also app-cover-height
  per index.css's own law (mobile-browser bottom band).
- SW pinned unhashed art until a version bump → stale-while-revalidate for non-/assets/ images.
- **DEFERRED**: trust-proxy hop count (needs Railway topology confirmation; correct for a
  single-proxy deploy, which Railway is).

## LOW — fixed
Login blur/base underline mismatch + form stacking; decompose error state + count-badge
invalidation + per-IP daily ceiling (the one LLM door outside the profile cap); yoga free-taste
race (reservation, released on failed generation); currentTransits rate-bounded;
setLocation lat/lon/tz validation; verdict lower-median + honest hinge fallback; rateLimit map
prune; yearRankCache drop-oldest cap; express.json 50mb→5mb; cookie regex boundary; rail
hard-cap at 5 slotted marks; guard.test duplicate import; stale "ghosted" comments; dead
per-cell constants (CROWN_YELLOW/ECLIPSE_RING/retroColor); rowGap comment corrected; orphan
login-gate-day/night.jpg removed.

## Deferred with reasons
- Premium per-endpoint rate limits (bounded by the 50/day profile cap).
- Offline app-shell + PWA-stranded-on-landing (SW/product decisions; landing has no app link
  by David's unlisted-app design — his call).
- First-load weight (~3-4MB art + 1.76MB monolith bundle) — an optimization pass, not a bug.
- Storage proxy per-user scoping (feature barely used; needs an ownership model).
- 8 pre-existing test failures (auth.logout cookie shape, birthchart×3, dasha×2, profiles,
  projects) — identical at HEAD in both timezones: broken MOCK EXPECTATIONS, not broken code.
  Known test debt; untouched by and unrelated to this repair.

## Standard check
David's bar: "only low errors at each audit." This run: 1 CRITICAL + 7 HIGH — above the bar,
and almost all of it born in the v680–v720 sprint (the newest code). The lesson feeding
forward: velocity without the /audit matrix + noUnusedLocals class of guards mints HIGHs;
the harness caught none of the render-losses (golden border, due label) because those
surfaces had no tiles. Tiles and lint-guards are the next hardening.
