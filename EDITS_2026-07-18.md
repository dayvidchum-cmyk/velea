# Field notes — 7/18/26 folder (status as of v765, 2026-07-19)

Every ✅ below was DRIVEN in a real browser (playwright) and verified — screenshots, DOM
overflow measurement, or DB check — before shipping. No blind claims.

## ✅ Fixed & verified (v765)
- **Chart page "Something went sideways" CRASH** — MeridianCard called hooks after an early
  `return null`; React hook-order error tanked the whole page via the error boundary. Fixed;
  Chart page renders. (This was the biggest one — a hard crash on a core page.)
- **"NOW" sits horribly / overlaps on the wheel** ("common issue on all profiles") — profection
  wheel hub widened 16→30px so the inner ring's two-digit ages stop touching. Verified rendered.
- **Jupiter glyph / crown-trio overflow** — year-calendar glyph cluster (crown + 2 planets) now
  steps down to fit its ~40px tile. Verified: 0 overflowing tiles across 171 measured.
- **Price + "notify me" button** — every locked feature now shows a "Notify me when it's live"
  gold button (waitlist row written + verified in DB) and a price line that stays HIDDEN until
  you set `PREMIUM_PRICING.monthly` in client/src/lib/pricing.ts (never an invented number).
- **"Matters" is banned** — purged from all user-facing copy.
- **Gold-on-dark / "go darker or lighter, don't touch the red card"** — the hero label + chevron
  now read off `hero-ink` (the old `day-accent-deep` went mud-on-mud on the red caution card).
  The red card itself is untouched.
- **Glossary links? No?** — the "how profection works" explainer now linkifies its terms.
- **Compress line spacing** — Chart page greeting→tabs→explainer run tightened.
- **Rename Morning Bell** (bad branding) → **First Light** (one constant, lib/brand.ts).
- **Pin/"Keep this reading" gave no feedback** (v764) — now flips optimistically on tap.
- **Login card glitchy / keyboard+nav dance** (v764) — dead-center modal, self-scrolling.
- **Push showed "from Velea" second line** (v764) — title is "Velea", greeting in the body.

## 🗣 Needs DAVID (design directions — will not guess per the never-assume-UX law)
- **"It's not September 18" / "reflection for Sep 18"** — the reflection log follows the SELECTED
  date; the code is correct but the selected-date grounding disorients. Part of the connected
  "how a reading opens + how the selected date grounds the user" direction that needs your spec.
- **"I have to add tasks that are aligned today? That's not the user's job"** — the empty-state
  asks the user to tag tasks by mode. Needs a rethink (auto-suggest from existing tasks?).
- **Fullscreen-art reading format as the default** — you love the label·headline·prose over the
  black-gradient surface. Needs your spec (pairs with the Sep-18 grounding above).
- **Align27-style profile IA** — profiles as a named stack; "Manage Profiles" beside them.
- **"Prosperity day, but the writing doesn't say it"** — needs the specific case pulled.

## ⏳ Not yet reviewed
- **"So many things wrong.mp4"** (470MB) — not watched yet (large file). NEXT session, first thing.
