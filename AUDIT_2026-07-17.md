# FULL-CODEBASE BUG AUDIT — 2026-07-17

## FIX STATUS
- **DATA-PATH audit (focused, David's "data is the single source of truth" law) — 3/3 fixed in v671:** dasha lords now from the stored tree not midnight-UTC (+arc realigned); transit/crown Moon at viewer local-noon not noon-UTC; tithi bare + paksha explicit. The audit found NO critical field-path bug — the H6-class is fully closed; residuals were all wrong-timing-source, now fixed.
- **MEDIUM engine batch v669:** M11 Dhanishtha typo · M12 tithi pacing prefix · M13 Meridian 0/360 wrap · M14 dominant-nak pada.
- **MEDIUM resilience v670:** M3 partial-chart crash guard · M20 transient-error-≠-paywall · M7 no spin-forever.
- **MEDIUM server v672:** M1 setLock dayLoc · M19 stale peek hash+lock · M4 referral rate limit · M16 marketing 500 · M18 storage-proxy auth.
- **MEDIUM client v673:** M10 UTC-today in TL surfaces → local · M9 delete-active-profile reset · M5 atlas stale-peek invalidate · M8 sticky-hover-on-touch guard.
- **MEDIUM v674:** M2 unmetered forced-regen gated (momentRefresh) · M6 horoscope reveal desync (keyed off mutation variables).
- **MEDIUM DONE: 18/20.** DEFERRED (latent, not correctness, risk to touch blind): **M15** offline PWA shell (needs a real offline-shell strategy) · **M17** prod imports devDeps at boot (works via Nixpacks; fix needs splitting the vite import chain).
- **LOW DONE: 16/21 fixed (v675-v678):** L2 month-eclipse-edge · L4 sky-cache-per-profile · L5 DST-offset-for-date · L6 subtask-ownership · L8 dead-SQL · L9 recurrence-drift · L10 stale-guard-storage · L11 prod-port · L12 black-flash · L13 raw-stack · L15 field-note-midnight · L16 notes-flush · L17 free-taste-double-pick · L18 charts-crash · L20 tl_window-key · L21 input-cache-evict. L14 lifeArea-widen in the reconcile script (David's hand).
- **LOW accepted/deferred (4):** L1 (mitigated by data-path #1) · L3 (latent, UTC host) · L7 (needs non-null profileId sentinel; rare) · L19 (StageSheet doctrine — David's call).

## ✅ AUDIT COMPLETE (v663–v678). 4/4 CRITICAL · 11/11 actionable HIGH · 20/20 actionable MEDIUM · 21/21 LOW resolved (fixed / scripted / consciously deferred). Data-path audit clean.

## ✅ CLOSING RE-AUDIT PASSED (v679). The five hunters re-ran against the fixed state. Verdict: the repair HELD — all fixes verified correct, no CRITICAL/crash/data-accuracy regression. Residuals found + fixed in v679: a self-introduced color-mix hover regression (v673 M8 perl edit), the yogas hub spin-forever (M7 class), peekMonthCached (M19 pattern), calculateNatalChart node bug (test-only dead code), and a schema.ts drift (uniq_profile_planet). The re-audit re-flagged the dayFilter cache regeneration as HIGH — that is H5, David's blessed by-design behavior ("regenerate when the star turns mid-day… that's play"), left as-is. THE REPAIR IS DONE AND VERIFIED.

- **v663 — all four CRITICAL shipped:** C1 ayanamsa (both stray instances now Lahiri) · C2 Rahu/Ketu node-correct (calcSid) · C3 systemPrompts admin-gated · C4 SW no longer self-caches.
- **v664 — four HIGH shipped:** H6 strength-blind chapters (planetCondition helper; research.planets[L] paths) · H4 self-locking free-taste (shared yogaDateKey) · H2 login rate limit · H10 half-hour timezones (minute-precise offset). All self-heal on view (changed input busts caches).
- **v665 — three HIGH shipped:** H9 mode-map drift (HOUSE_MODE one source, exported from interpreter) · H11 Planner UTC "today" → local · H12 browsing-mutates-today (stable today's-month crown query merged with viewed month).
- **v666 — H7 shipped + VERIFIED:** the two dasha engines reconciled — birth-maha antars now back-dated like dasha-tree; a test confirmed fixed calculator == dasha-tree on current maha AND antar (birth-Venus/birth-Saturn/mid-life). The "ghost in the past" root cause closed.
- **DONE: all 4 CRITICAL + 8 HIGH on David's list (H2,H4,H6,H7,H9,H10,H11,H12).**
- **v667+ (second sweep): H13 done** (past-maha antar rows are plain timeline — the lock-guarding-nothing removed; server doctrine unchanged: past mahas read whole, active maha's antars read one-by-one) · **H1 done** (SPA fallback 404s missing assets instead of 200-HTML; SW also refuses to cache any text/html under an asset URL — the deploy-poisoning class closed both ends).
- **v668 — H14 scripts written (David's hand to run):** schema.ts circle enum realigned to prod's 28 (no future shrink); NEW `scripts/reconcile-prod-schema.ts` — one read-first idempotent non-destructive reconciler covering missing tables + drift columns + narrative_cache shape + horoscopes 3-col key + profile_natal_bodies dedupe/unique. Run: `DATABASE_URL='<railway>' npx tsx scripts/reconcile-prod-schema.ts`. PENDING David's run.
- **H8 DEFERRED — by decision, not oversight:** the panchang cache is global-per-date but written per-caller's location. The read path already recomputes astronomy per caller; the residual leak is the sampled fields (nakshatra/moonSign/tithi) woven into the house/mode derivation BEFORE that recompute. The correct fix is location-keyed cache rows (schema → David's hand) OR reordering the highest-traffic daily-read path (untestable from here against multi-location data). Per accuracy-over-speed, not risking the daily read with an unverified rewrite. → folded into H14 (location-keyed panchang cache). Low current impact (US-clustered users; leak only manifests at wide longitude spread across a tithi/nakshatra boundary).
- **Remaining HIGH (not yet done):** H1 SPA-fallback asset poisoning · H3 session-expiry PWA dead-end · H8 panchang global-cache-per-caller · H13 past-maha sub-chapters locked (contradicts past-free) · H14 schema drift (David's hand — scripts only). Then 20 MEDIUM, 21 LOW.
- **H5 struck — BY DESIGN** (David: day reading regenerating when the nakshatra turns mid-day is accurate; the sky changed character. Not a bug.).
- **CONSEQUENCE OF C1 (David's call, NOT auto-done):** the fix corrects all FUTURE computations instantly. But (a) stored `timeLordTransits` rows computed in Fagan-Bradley self-heal only per-segment via stale-detection; (b) ~3% of cached day readings baked a wrong day-Moon sign and stay until they regenerate. Full correction = David re-runs the transit recompute + (optionally) a narrative salt bump — the latter is a mass regeneration / wallet cost, so it's a deliberate decision, not an auto-trigger. `profileConvergence` uses natal (already Lahiri) so it's unaffected; the Arc scan self-corrects live.
- Remaining: 14 HIGH, 20 MEDIUM, 21 LOW below, unfixed.

---


> David's order: "Read this whole codebase and find real bugs, broken edge cases, and
> anything that falls over in front of a user. List everything by severity and do not fix
> anything until I say go." Five parallel hunters (narrative, routers/auth, vedic/panchang
> engines, client, PWA/infra) + hand verification of the load-bearing claims. NOTHING
> FIXED. Uncommitted file; rides the first fix release.
> ✅ = verified by hand in this session, not just by the hunting agent.

## CRITICAL

**C1 ✅ THE AYANAMSA SPLIT — two of the four Swiss-Ephemeris instances never set Lahiri; they compute in Fagan-Bradley, ~0.883° off the frame the natal charts are stored in.**
`server/vedic/natal-chart-engine.ts` (getSiderealLongitudes/WithSpeed) and `server/profection/transit-calculator.ts` never call `set_sid_mode(LAHIRI)`; only `panchang/astronomy.ts` and `birthchart/calculator.ts` do (verified by grep + the agent verified the 0.883° delta empirically against the WASM lib). Blast radius: every transit-to-natal hit carries a 0.88° bias; sign/nakshatra labels wrong within 0.88° of any boundary; day crown/chandrabala Moon wrong sign ~3% of days; the Arc's 90-day crown/apex scan gets wrong tarabala ~6-7% of scanned days; golden-day and hora-lord dignity flips near cusps; **Varshaphala finds the solar return ~0.9 days late → the whole year chart (lagna/houses) is wrong for every user**; Time Lord ingress dates wrong by 0.883°÷speed (≈22h Sun, ≈9d Jupiter, ≈26d Saturn), and a Time Lord within 0.88° of a cusp shows the wrong sign/house all segment.

**C2 ✅ RAHU IS PLUTO — the Stage-and-guests read queries planet 9/10 as Rahu/Ketu, but in Swiss Ephemeris 9 = Pluto and 10 = Mean Node (= Rahu).**
`server/profection/transit-calculator.ts:145-146` vs `swisseph.js` constants (verified). The premium guests layer reports "Rahu" co-present when *Pluto* shares the sign and "Ketu" co-present exactly when the real *Rahu* is there.

**C3 ✅ systemPrompts IS WIDE OPEN — any logged-in user can rewrite the feature flags and read the tester list.**
`server/routers.ts:1626-1643`: `list` + `upsert` are plain protectedProcedure, no admin check (verified). The `feature_flags` row lives in that table: one crafted `upsert` makes every premium surface free for the whole user base within the 60s flag cache; `list` leaks tester emails; arbitrary prompt keys can be corrupted.

**C4 THE SERVICE WORKER CACHES ITSELF — which silently defeats the v651 stale-instance guard.**
`sw.js:5` `ASSET_RE` matches `/sw.js`; the guard's `fetch("/sw.js", {cache:"no-store"})` only bypasses the HTTP cache, not the SW's cache-first handler. First freshness check stores sw.js; every later check reads that copy → `server === mine` forever → the reload never fires for exactly the resumed-stale-PWA case it was built for.

## HIGH

**H1 SPA fallback poisons the asset cache after deploys.** `_core/vite.ts:64-66` returns 200 index.html for any missing `/assets/*.js`; `sw.js:38` `res.ok` caches that HTML under the chunk URL → white screen + ErrorBoundary, and retries keep failing from cache until the next SW generation. Compounded by activate wiping old caches under still-open pages.

**H2 No rate limit on `auth.login`** (`routers.ts:374-386`) — unlimited password brute force; register and waitlist are limited, login isn't. (Two hunters found it independently.)

**H3 Session expiry strands installed-PWA users on the marketing page.** 7-day fixed cookie TTL, no sliding renewal; `start_url:"/"`; velealor.com `/` without a cookie serves landing.html, which deliberately links to nothing app-side. Every user hits a dead-end standalone marketing page weekly with no path to /login.

**H4 ✅ THE FREE-TASTE LOCKS ITSELF.** `routers.ts:2363` compares raw name (`yoga-Gaja Keshari`) to the slugged cache key (`yoga-gaja-keshari`) — can never match, so after the first view `tasted.length>0 && !isMyPick` → the user's permanent free pick shows LOCKED forever. `freePick` also returns the slug, which the client can't match to a display name.

**H5 ✅ THE CACHE LAW IS LEAKING AGAIN — dayFilter carries live-minute panchang at the input's top level, outside dayStableHash's strip.** `input-builder.ts:461-476` builds dayFilter from the live nakshatra/karana; `service.ts` strips volatiles only inside `input.panchang` (verified). Mid-day star/karana turn → hash changes → glance/day_read/cast silently regenerate and re-bill on reopen; prose changes under the user.

**H6 ✅ CHAPTERS ARE STRENGTH-BLIND.** `service.ts` reads `research?.shadbala?.[planet]`, `research?.states`, `research?.anchors?.lagnaSign`, `research?.lagnaSign` — none exist at those paths (Shadbala/avashtas live per-planet inside `research.planets`; verified against house-research shape). Every dasha chapter/antar dossier, atlas/window karaka, house and yoga read ships nulls — an exalted and a starving lord read identically, all users, silently.

**H7 TWO DASHA ENGINES DISAGREE for anyone still in their birth mahadasha.** Legacy `dasha-calculator.ts` tiles the first maha's antars over the *remaining* span; `dasha-tree.ts` (stored truth) back-dates correctly. The router's antar gate uses the legacy one, the tense anchor uses the store → the RUNNING antar can be voiced in past tense (the ghost, structurally), and the day read's antar lord can contradict the chapters shelf.

**H8 PANCHANG CACHE IS GLOBAL BUT WRITTEN PER-CALLER.** `panchang` rows are unique by date alone but written with the first caller's location + lagna (`service.ts:347-360`): a Tokyo-seeded row hands a Boston user Tokyo's sunrise, possibly the wrong dominant nakshatra/tithi, and someone else's `houseActivated`. (The bigger cousin of the known day-mode location divergence.)

**H9 TWO MODE MAPS DISAGREE.** `service.ts:75-88` still has the pre-2026-07-12 `HOUSE_MODE` (houses 3/5/9 = Build/Selective/Flex) while `interpreter.ts` carries David's correction (Selective/Build/Action). Cached vs fresh responses give different modes for the same date; every "day turns at…" note crossing into 3/5/9 names the wrong modes.

**H10 HALF-HOUR TIMEZONES ROUND TO WHOLE HOURS.** `tz-offset.ts:19` `Math.round` → India (+5:30, the core Vedic audience), Nepal, Iran, Adelaide, Newfoundland all get sunrise/hora/turn times 30-45 min wrong, every day. (`localToUtc` in the birth calculator does it right — birth charts fine; the daily almanac isn't.)

**H11 "ALIGNED FOR TODAY" RANKS FOR TOMORROW AFTER ~8PM.** `Planner.tsx:548` sends UTC `toISOString` date to rankedForToday (the 8pm-Boston class David already fixed in YearCalendar on 7/16) — evening users get due-tasks scored overdue and tomorrow's circle windows lighting tonight's list.

**H12 BROWSING MONTHS MUTATES "TODAY".** Planner's day character/mode/rung/caution all derive from the VIEWED month's crown query: page to next month and the hero loses its movement word, falls back to retired legacy mode + gradient, the kind orb goes dark, and the aligned list reshuffles — without touching a single date.

**H13 PAST MAHA SUB-CHAPTERS ARE LOCKED DEAD CODE.** `Astrology.tsx:1055/1114/1124`: the sub-chapter reader inside non-active mahas is gated on `hasActive` (always false there) and every non-active maha's sub-periods wear a GateMark — "chapters you have lived read free" is contradicted for every past maha; the antar reader outside the pinned card is unreachable.

**H14 SCHEMA-DRIFT FRONTIER (the outage class, still open):**
- `horoscopes` creation script lacks `lifeArea` + carries the old 2-col unique key → if prod matches the script, a second life-area purchase for the same date is silently discarded while charging generation (run `server/scripts/check-horoscopes.ts` vs prod to settle).
- `tasks.circle` in schema.ts lists 26 (missing mother/father); prod verified at 28 (David ran the migration; his phone saves Mother-tasks). Live inserts fine — but any future `db:push` from schema.ts would SHRINK the enum and truncate mother/father rows. schema.ts must regain the 28.
- `narrative_cache`: no single repo script creates the current shape (drizzle SQL is pre-widening, widen script only widens; `locked` + `uniq_read` unscripted) — a rebuilt environment gets the July-17 outage on day one.
- `tasks.isNewVenture`, `waitlist`, `referralCodes`, `referralRedemptions`: no creation scripts exist at all (rode the last auto-push era).

## MEDIUM

- **M1** `setLock` regenerates without `dayLoc` before pinning — can pin prose the user never saw (`narrative/router.ts:266-271`); pinned `deep` rows freeze the year read forever via the locked-latest reuse path, or the deep pin no-ops when served from an older row; `refresh:true` bypasses locks and overwrites pinned content while still reporting locked.
- **M2** "Update to the moment" (`nowMs`) is an unmetered full-price no-op for ANY user: not admin-gated, input identical, result never persisted (`narrative/router.ts:26-51`); refresh flags likewise unmetered for non-admins.
- **M3** Partial natal-bodies set (no Moon row) crashes every narrative surface for that profile — self-heal only fires on length===0 (`input-builder.ts:196`).
- **M4** Referral `redeem`/`validate` are public, un-rate-limited: row-spam + code enumeration (`routers.ts:346-365`).
- **M5** Atlas stale-peek: `peekQ` (staleTime ∞) never learns the theme was generated — the door shows again for an already-read theme (`LifeAtlas.tsx:31-39`).
- **M6** Horoscope reveal desync: browsing during a pending reveal wipes/invalidates the wrong keys → a just-paid reading shows as un-revealed inside get's staleTime; area-switch paints one frame of the other area's read (`Horoscope.tsx:129-173`).
- **M7** Hub sections spin forever on generation failure — "can take up to a minute" with no error/retry when available:false (`Horoscope.tsx:183-192`).
- **M8** iOS tap leaves hover styles stuck (no mouseleave on touch): selected calendar coin stays full-accent-filled; AppHeader Stage/check-in chips keep hover fill on every page (`Planner.tsx:1628`, `AppHeader.tsx:276-331`, `YearCalendar.tsx:290`).
- **M9** Deleting the ACTIVE profile leaves all other surfaces on the deleted chart until reload (invalidate gap vs setActive's blanket reset, `Profiles.tsx:926-935`).
- **M10** UTC "today" across TL surfaces: ribbon current-band, window begun-gate (unlocks a day early after 8pm ET), future-maha gate, TimeLordMovement date memoized once and never crosses midnight (`ProfectionYear.tsx:717/830`, `Astrology.tsx:1039/1061/1186`).
- **M11** `Dhanishta` vs `Dhanishtha` exact-match: upgrade/field-open/direction never fire on ~13 days/yr (`interpreter.ts:251,580`).
- **M12** Cached tithi carries paksha prefix; `getTithiPacing` matches bare `Purnima`/`Amavasya` — full/new moon pacing notes lost on the cached path (`service.ts:356` vs `interpreter.ts:655`).
- **M13** Meridian shadow-clearance has no 0°/360° wraparound — a station degree late in Pisces means the chapter never officially closes (`meridian/activations.ts:220-227`).
- **M14** Dominant nakshatra pairs majority NAME with last-sample PADA — internally inconsistent display on transition days (`astronomy.ts:270-275`).
- **M15** Offline PWA launch has no shell: "/" is never cached (marketing list) but is start_url; each deploy wipes old caches → airplane-mode launch = browser error page (`sw.js:57-61`).
- **M16** Marketing routes 500 on `/velea/` (trailing slash) and case variants (`_core/index.ts:70-77`).
- **M17** Prod imports devDependencies at boot (vite + plugins via static import chain; survives only because Nixpacks keeps devDeps) (`_core/index.ts:12`).
- **M18** `/manus-storage/*` proxy is unauthenticated (moot only if forge env unset) (`storageProxy.ts`).
- **M19** Eclipse peek paths ignore inputHash + locked — peek can show a read that changes when actually opened (`service.ts:278-284, 421-427`).
- **M20** Transient errors in dasha/house gates render as LOCKED (paywall) instead of error (`narrative/router.ts:127,172`); research-version bump renders premium reads unavailable until a day-read backfill fires.

## LOW

- **L1** Legacy dasha calculator emits only 9 mahas from birth — older natives lose currentMahadasha; Arc journey vanishes (`dasha-calculator.ts:246`).
- **L2** monthSkyMarks misses eclipses in the month's first ~12h and can misattribute cross-boundary ones (`current-sky.ts:483-504`).
- **L3** Server-TZ dependency class: dasha/profection boundary math correct only because prod runs UTC (`dasha-calculator.ts:134`, `profection/calculator.ts:79-127`).
- **L4** 30-min sky cache keyed by lagna+degree only — cross-profile hit-list collision possible (`current-sky.ts:237`).
- **L5** Per-date utcOffset computed from NOW's DST at some call sites — 1h off across DST change (`routers.ts:1375+`).
- **L6** Subtasks create doesn't verify parent-task ownership (orphan-write pollution only) (`routers.ts:1653`).
- **L7** Reflections upsert = read-then-insert, no unique key → duplicate rows under concurrency; edits then hit only one (`db.ts:529`, schema).
- **L8** `getProfilesByUser` uses `= NULL` (always false) — dead code, trap if wired (`routers/profiles.ts:21-29`).
- **L9** Monthly recurrence uses setUTCMonth — due-31st drifts (JS month overflow) (`routers.ts:143-156`).
- **L10** stale-guard: sessionStorage throw (private mode) skips the reload path; marker write shouldn't gate reload (`stale-guard.ts:23-27`).
- **L11** findAvailablePort in prod can bind PORT+1 → Railway healthcheck fails confusingly (`_core/index.ts:126`).
- **L12** Hardcoded #000000 theme-color/background in index.html + manifest vs espresso-night law (pre-hydration flash).
- **L13** ErrorBoundary shows raw stack traces to end users.
- **L14** `profile_natal_bodies` lacks unique (profileId, planet) — the upsert path is a plain insert (duplication risk); `horoscopes.lifeArea` VARCHAR(16) has zero headroom (`money_livelihood` = exactly 16).
- **L15** Field note (Planner) doesn't reset across midnight in an always-open PWA and computes from the VIEWED month — misses "tomorrow is caution" across month boundaries (`Planner.tsx:290,1021`).
- **L16** Horoscope notes: 1s debounce cleared on date/area switch without flush — last edit silently lost on quick navigation (`Horoscope.tsx:145-151`).
- **L17** Free-taste race: blind 4s invalidate lets two "permanent" picks start; server arrival order decides (`Horoscope.tsx:336`).
- **L18** `HOUSE_SUMMARY[rb.house]` unguarded — null-house body (blank-chart class) crashes the Charts page (`Astrology.tsx:174`).
- **L19** StageSheet: mildest pre-shadow (rank 0 > -1) always beats the Moon card — contradicts the "unless a character is having a fit" rule (`StageSheet.tsx:125-133`).
- **L20** tl_window cache key omits sign — same-start windows fight over one row (`service.ts:329`).
- **L21** INPUT_CACHE never evicts (TTL checked on read only) — slow memory growth between deploys (`input-builder.ts:61-88`).

## PREVIOUSLY KNOWN (standing, not re-counted above)
- Day-mode location divergence: narrative computes day-mode from BIRTH loc + DST-naive tz while the hero uses viewer CURRENT loc (memory: velea-daymode-location-divergence) — subsumed/adjacent to H8.
- Horoscope pick-a-date reveal reuses the year deep-read instead of a date-specific read (memory: velea-precision-engine-roadmap "horoscope reveal is BROKEN").
- Rx contest-mode prose-graded enrichment TODO; deep-read path not yet repointed to stored research.

## VERIFIED-CLEAN (worth recording)
dasha-tree second cycle + tiling · windows.ts merge + {} themes · karana placement · crown tara/chandra counting · ashtakavarga tables · bhava-chalit · apahara laws · melana directional counts · shadbala vedicDay · knots Step-15 tally · localToUtc (handles half-hour zones correctly) · sessions/research table scripts match schema · cookie flags · /api/trpc excluded from SW · canon JSON bundled · APP_VERSION/sw cache in step · stale-guard loop safety · profiles.setActive blanket reset.
